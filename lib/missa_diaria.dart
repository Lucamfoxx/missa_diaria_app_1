import 'package:flutter/material.dart';

class MissaDiariaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Missa Diária'),
      ),
      body: Center(
        child: Text('Conteúdo da Missa Diária'),
      ),
    );
  }
}
