import 'package:flutter/material.dart';

class SantoDoDiaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Santo do Dia'),
      ),
      body: Center(
        child: Text('Conteúdo do Santo do Dia'),
      ),
    );
  }
}
